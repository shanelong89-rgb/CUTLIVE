kind = "web"
previewPath = "/"
title = "CULTIVE"
version = "1.0.0"
id = "artifacts/cultive"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20588

[services.development]
run = "pnpm --filter @workspace/cultive run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cultive", "run", "build" ]
publicDir = "artifacts/cultive/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20588"
BASE_PATH = "/"
